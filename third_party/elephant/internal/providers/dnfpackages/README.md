### Elephant DNF Packages

Find and manage packages using DNF.

#### Features

- Search for packages in configured repositories
- Install/Remove single packages
- List all packages or installed packages

#### Requirements

- `dnf` package manager (tested with dnf5 and Fedora 43)
